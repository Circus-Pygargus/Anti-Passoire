TODOS :
_ recherche infructueuse :
    afficher un truc du genre :
     Pas de résultats, change de catégorie ou choisis d'autres mots clés ...

_ affichage un antiPassoire :
    - ajouter Créé le et dernière édition (si différent)

_ page liées à User (login, enregistrement, modif pwd) :
    - revoir les vues

_ faire les pages 404, 503 ...

_ changer le titre 'Création d'un anti passoire'
    en profiter pour changer 'Texte *' par Contenu

_ mixin pour screen > 1600, reprendre avec une base de 1600 de large

_ /admin/user/list : ajouter un spinner de chargement si bouton de confirmation de modif de rôle cliqué
    car la page est un peu longue à recharger

_ ajouter l'envoi d'email

_ s'occupper des AntiPassoire
    ::displayNbr et ::lastDisplay   FAIT
    des mots clés pour les metadatas aussi
    en gros revoir pour afficher tout le contenu ..

_ à la suppression d'un CategoryGroup, les catégories associées passent dans le groupe public ? sont supprimées ?
    Faire une pop up listant les catégories pour laisser le choix ?

_ liste de groupes de catégories : coller un formulaire pour les boutons d'édition

_ CategoryGroup :
    - Ajouter la colonne isLabelEditable (pour Public et _Privée_...)
        ou peut pet-être etre fait^dans le formType avec un disabled sur une fonction qui checke
    - Ajouter la colonne comment ou explainationText plutôt,
        pour garder comments pour l'ajout futur des commentaires
    - Interdire la création ayant comme label Public ou commençant par un mot contenant privé ou prive (après lowercase)
        -> utiliser le slug =)

!! _ trouver un moyen d'ajouter le groupe privé à l'admin (moi) en prod

BUGS :

_ pas forcément un bug, mais truc chiant :
    searcher -> requête -> bouton revient sur plus même si sur - avant requête
